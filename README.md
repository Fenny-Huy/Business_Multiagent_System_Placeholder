this system requires the chroma server from G1 system to be active

to install the conda environments

```bash
conda env create -f langchain-demo-env.yml -y
```

then activate the environment
```bash
conda activate multi-agent-env
```

create the .env file from the sample file .env.sample 

to run the system
```bash
python multiagent_system.py
```
